<h3>Name- <%= @item.name %></h3>
<h3>Price- <%= @item.price%></h3>
<%= image_tag(@item.image)%>
<% if @item.user_id != session[:user_id] %>
    <%= link_to "Borrow", create_booking_path(item_id: @item.id) %>
<%end%>